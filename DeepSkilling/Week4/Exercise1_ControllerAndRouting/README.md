# Exercise 1: Controller & RESTful Routing

## 📌 Problem Statement
Create an ASP.NET Core 8.0 API Controller for managing an Inventory Product Catalog. The controller must implement standard RESTful HTTP actions (`GET`, `POST`, `PUT`, `DELETE`) with appropriate HTTP status codes.
