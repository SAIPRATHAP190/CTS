# Exercise 5: JWT Authentication & Authorization

## 📌 Problem Statement
Secure API endpoints using JSON Web Tokens (JWT). Users authenticate at `POST /api/auth/login`, receive a signed JWT token, and present it in HTTP headers: `Authorization: Bearer <TOKEN>`.
